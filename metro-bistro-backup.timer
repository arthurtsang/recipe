[Unit]
Description=Run Metro Bistro backup daily at 2 AM (legacy — use Vercel cron /api/cron/backup in production)
Requires=metro-bistro-backup.service

[Timer]
OnCalendar=*-*-* 02:00:00
Persistent=true

[Install]
WantedBy=timers.target 