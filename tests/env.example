# Copy to tests/.env (gitignored) for local BDD runs against preview.
BASE_URL=https://recipe-preview.youramaryllis.com
API_TOKEN=your_64_char_hex_api_token

# Optional: skip deleting test recipes/jobs after @api scenarios
# BDD_CLEANUP=0

# Optional: for legacy @browser scenarios that still use Google OAuth storage state
# STORAGE_STATE=./storage-state.json
