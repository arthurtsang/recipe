FROM python:3.11-slim

WORKDIR /app

COPY mock_ai_service/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY mock_ai_service/app.py ./app.py

EXPOSE 8001

CMD ["python", "-m", "uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8001"]
